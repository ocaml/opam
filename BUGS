* 'ocp-get remove' does not remove forward dependencies

* nothing should be linked from ../build/.. directories.
  these folders are supposed to be temporary, ie. they
  will be deleted when we need to make some on cleanups
  later

* misc files are not deleted