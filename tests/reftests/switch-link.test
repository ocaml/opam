N0REP0
### OPAMYES=1
### : make sure opam switch link doesn't remove previous switches
### opam switch create empty --empty
### opam switch create empty2 --empty
### opam switch link empty2
Directory ${BASEDIR} set to use switch empty2.
Just remove ${BASEDIR}/_opam to unlink.
### opam switch link empty
Directory ${BASEDIR} set to use switch empty.
Just remove ${BASEDIR}/_opam to unlink.
### opam switch
#   switch  compiler  description
->  empty             empty
    empty2            empty2
### rm _opam
### opam switch
#   switch  compiler  description
    empty             empty
->  empty2            empty2
### : make sure that opam switch link doesn't remove already existing local switch
### opam switch create empty3 --empty
### opam switch create . --empty
### opam switch link empty3 .
[ERROR] There already is a local switch in ${BASEDIR}. Remove it and try again.
# Return code 2 #
### opam switch show
${BASEDIR}
### opam switch -s
${BASEDIR}
empty
empty2
empty3
